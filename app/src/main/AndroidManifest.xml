<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notificaciones">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notificaciones">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".SignIn" />
        <activity android:name=".Login.SignIn" />

        <service
            android:name=".lib.FirebaseInstanceIdService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".lib.FirebaseInstanceIDService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".lib.MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".lib.FirebaseInstanceIDService" />
    </application>

</manifest>